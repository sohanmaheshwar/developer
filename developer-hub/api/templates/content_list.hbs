[
        {{#each (content_filter site.pages "/content/documents/" "hub_document")}}
            {
            "title": "{{this.page.head.title}}",
            "category": "{{this.page.head.extra.category}}",
            "language": "{{this.page.head.extra.language}}"
            }{{#if @last}}{{else}},{{/if}}
        {{/each}}

]